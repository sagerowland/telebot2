services:
  - type: web
    name: telebot2
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install
    startCommand: python telebot2/scraper_handlers.py
    plan: free
